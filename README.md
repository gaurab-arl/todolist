# todolist
    Set align-items: start on .container-contain to prevent grid items from stretching each other.
Add overflow-y: auto to .time-stamp so it behaves like .container when content exceeds max-height.